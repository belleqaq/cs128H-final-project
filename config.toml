# ============================================================
# Brownshock — macroquad prototype settings
# ============================================================
#
# This version is a fixed-camera room prototype.
# Save the file and re-run the game to apply changes.
#
#   cargo run
#
# or, if you are still using the project Docker image only for builds:
#
#   docker compose run --rm dev cargo check
#
# The native macroquad window must run on your host OS, not inside Docker.
# ============================================================

[window]
width = 1280
height = 720
title = "Brownshock"

[player]
speed = 210.0
radius = 18.0
